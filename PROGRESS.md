# Progress

- [ ] SMDB-1 Update swift-memory to released DatabaseKit 26.0831.1 and Database Framework 26.0905.0 while preserving its persisted schema identities, MultiBase isolation, authorization, and provenance contracts `depends:none` `parallel:none`
  - [x] SMDB-1.1 Record one minimal package design contract: Memory supplies a configured root path and delegates StorageKit Directory and framework-format admission to `DBContainer.open`; legacy tuple-namespace and unformatted nonempty stores retain the upstream typed incompatibility failure without import or reinitialization, and swift-memory never duplicates private framework paths `depends:none` `parallel:none`
  - [x] SMDB-1.2 Raise the two package requirements, resolve a release-only dependency graph, adapt the public topology initializers, and remove swift-memory's obsolete raw-key layout probe so `DBContainer.open` owns admission; do not edit sibling database repositories or change Given, Statement, Trace, Entity, application #Directory, index, graph, field, schema-version, or execution-identity encodings `depends:SMDB-1.1` `parallel:none`
  - [x] SMDB-1.3 Build the SwiftMemory and MemoryOntology targets, then run timeout-bounded behavioral tests covering store, resolve, recall, authenticated Base provisioning, equal-ID layer isolation, Composition origin/order, new-format SQLite reopen, and explicit legacy/unformatted-root rejection; add only the smallest regression needed where existing tests do not exercise the changed admission path `depends:SMDB-1.2` `parallel:none`
  - [ ] SMDB-1.4 Review the scoped diff and dependency graph for obsolete database APIs, local-path overrides, private layout duplication, silent compatibility fallbacks, and changes to the fixed persistence/runtime identities, then create one coherent local commit containing only this sprint `depends:SMDB-1.3` `parallel:none`
- [ ] SMDB-2 Confirm the committed tree exactly matches the already verified source and resolved release graph, retain the valid build and behavioral-test evidence without redundant reruns, verify the worktree and upstream relationship are safe, and push only when no unrelated unpushed commits would be included `depends:SMDB-1` `parallel:none`
